{
  "name": "hush",
  "version": "0.1.0",
  "private": true,
  "description": "Hush — a horror web game (monorepo)",
  "workspaces": [
    "shared",
    "client",
    "server"
  ],
  "scripts": {
    "predev": "npm run build -w shared",
    "dev": "concurrently -n server,client -c magenta,cyan \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:client": "npm run dev -w client",
    "dev:server": "npm run build -w shared && npm run dev -w server",
    "build": "npm run build -w shared && npm run build -w server && npm run build -w client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
